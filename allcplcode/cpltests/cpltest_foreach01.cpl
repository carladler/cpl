entry  fn hello(args){
	if Length(args) == 0{
		print "This test requires at least 1 command line argument";
		return 0;
	}
	a=[2,"foobar",4];
	b = [a[0],200,300];

	foreach arg args{
		print arg;
	}
	
	foreach i a{
		print i;
	}

	foreach x b{
		print x;
	}
}